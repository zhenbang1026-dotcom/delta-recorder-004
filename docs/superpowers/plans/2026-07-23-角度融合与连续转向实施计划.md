# 角度融合与连续转向实施计划

## 任务 1：建立测试基线与 Legacy 保护

- 新建测试目录和模块加载辅助工具。
- 写失败测试：默认模式、Legacy 参数、Legacy 路线抽稀结果、Legacy 动作选择结果。
- 保存修改前关键 Legacy 函数的源码哈希或等价行为样例，后续验证分支没有改变。
- 验证：测试先因缺少新能力失败，Legacy 基线断言从一开始通过。

## 任务 2：实现 TEXT MAP 圆形插值

- 写失败测试：标定点、相邻插值、359°/0°首尾插值、无 MAP 回退、真实 353 样本留一误差。
- 在 `识别角度.py` 增加最小的纯函数/缓存数据结构，并把识别输出接到映射结果。
- 在最近详情中标记 `map` 或 `fallback`。
- 验证：相关测试转绿；真实样本留一平均误差与 P95 达标。

## 任务 3：新增 Fusion 观察器与模式接入

- 写失败测试：模式正规化、Fusion ROI、一致观测、明显分歧、连续确认、TEXT/Legacy 单方失败、双方失败。
- 新建纯 Fusion 选择器并在角度分发层同时调用两种识别。
- 只在新分支裁切 Legacy ROI，不修改 Legacy 分析函数。
- 验证：Fusion 单元测试和三模式切换测试通过。

## 任务 4：实现 TEXT/Fusion 连续控制

- 写失败测试：死区为零、最大速度、最大增量、方向翻转先过零、重置行为、分段总和严格等于目标。
- 增加纯控制整形器与确定性分段移动函数。
- 在巡航控制器和执行器中仅对 TEXT/Fusion 分流；Legacy 继续调用原 `丝滑相对移动`。
- 验证：控制器测试通过，Legacy 执行器调用快照不变。

## 任务 5：优化高级模式路线与近点保护

- 写失败测试：Legacy 仍按 18 抽稀，高级模式按 6；高级模式近点不产生大转向；Legacy 原结果不变。
- 最小修改路线读取和近点动作分流。
- 验证：路线/动作测试通过。

## 任务 6：接入 UI、日志和文档

- 给合并主界面和独立巡航入口增加 Fusion 单选项和说明。
- 默认 `StringVar` 与初始化参数保持 `legacy`。
- 更新 README 的三模式与控制说明。
- 验证：静态导入、模式切换测试和编译检查通过。

## 任务 7：全量复验与交付

- 运行 `python -m pytest -q`。
- 运行 `python -m compileall -q .`。
- 运行真实标定离线评估和控制序列仿真。
- 比较 Legacy 关键代码/行为与基线，检查 `git diff --check` 和 `git status`。
- 只暂存本任务代码、测试和文档，使用中文提交消息提交并推送功能分支。
- 验证：远端分支存在，报告提交哈希、消息、分支和全部验证结果。
